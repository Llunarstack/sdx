"""
Reference data for prompts, domains, presets, and labels — **not** `TrainConfig` training hyperparameters.

Import submodules explicitly, e.g. ``from config.reference import prompt_domains`` or use the
shim modules at ``config.prompt_domains`` (same exports).
"""

__all__: list[str] = []

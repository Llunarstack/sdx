"""
Default data for prompts, domains, presets, and labels — **not** `TrainConfig` training hyperparameters.

Import submodules explicitly, e.g. ``from config.defaults.prompt_domains import ...``.
"""

__all__: list[str] = []

# Unit tests (`tests/unit/`)

Fast, isolated tests (no GPU). Prefer **`tests/diffusion/`** for diffusion-math suites.

Older tests may still live at the **`tests/`** root; new tests are grouped here over time.

# SDX optional Mojo entry — run with: mojo src/sdx_stub.mojo (from native/mojo, with mojo on PATH)
# Or: cd native/mojo && pixi run mojo-run (see pixi.toml)

def main():
    print("sdx native/mojo stub — extend for SIMD kernels or Pixi/uv Mojo env")

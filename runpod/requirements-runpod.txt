# Full Python stack for RunPod — installed by runpod/setup.sh
# Order matters: base ML stack, perf extras, then RunPod-specific packages.

-r ../requirements.txt
-r ../requirements-perf.txt
-r requirements-extra.txt

# QoL / scripts used across SDX (manifest IO, dashboards, preflight)
xxhash>=3.4.0
humanize>=4.9.0
psutil>=5.9.0

# Integration smoke (runpod/test.sh)
pytest>=7.4.0

# Native Rust Python bindings (optional build in setup.sh)
maturin>=1.4,<2

# Packaging helpers for editable / native builds
wheel
setuptools>=68.0

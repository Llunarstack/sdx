# NVIDIA CUDA 12.8 builds from PyTorch (torch, torchvision, xformers).
# PyPI’s default torch is often CPU-only; use this after the base install.
#
#   pip install -r requirements.txt
#   pip install --force-reinstall -r requirements-cuda128.txt

--extra-index-url https://download.pytorch.org/whl/cu128
torch
torchvision
xformers
